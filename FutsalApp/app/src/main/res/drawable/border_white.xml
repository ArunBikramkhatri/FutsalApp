<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Main shape without stroke -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="0dp"
                ></corners>
            <solid android:color="@color/grey" /> <!-- Transparent background -->
        </shape>
    </item>
    <!-- Bottom stroke layer -->
    <item android:bottom="2dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/fixturesColor" /> <!-- Transparent background -->
        </shape>
    </item>
</layer-list>